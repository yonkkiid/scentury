# 🔐 Настройка системы авторизации Scentury

## Обзор системы

Система авторизации включает в себя:
- **Форма входа** (login.php) - авторизация пользователей
- **Форма регистрации** (register.php) - создание новых аккаунтов
- **Личный кабинет** (dashboard.php) - управление профилем
- **Восстановление пароля** (forgot-password.php, reset-password.php)
- **Система аутентификации** (auth.php) - обработка всех запросов

## 🚀 Быстрый запуск

### 1. Запуск с файловым хранением (без БД)

Система автоматически переключится на файловое хранение, если база данных недоступна:

```bash
# Запустите сайт
php -S localhost:8000
# или
# Скопируйте в htdocs и откройте http://localhost/scentury/
```

### 2. Настройка базы данных (рекомендуется)

1. **Создайте базу данных:**
   ```sql
   CREATE DATABASE scentury CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

2. **Импортируйте структуру:**
   ```bash
   mysql -u root -p scentury < database.sql
   ```

3. **Настройте подключение в auth.php:**
   ```php
   $host = 'localhost';
   $dbname = 'scentury';
   $username = 'root';
   $password = 'ваш_пароль';
   ```

## 📱 Демонстрация форм

### Форма входа (login.php)
- **URL:** http://localhost/scentury/login.php
- **Функции:**
  - Валидация email и пароля
  - Опция "Запомнить меня"
  - Ссылка на восстановление пароля
  - Обработка ошибок

### Форма регистрации (register.php)
- **URL:** http://localhost/scentury/register.php
- **Функции:**
  - Поля: Имя, Фамилия, Email, Телефон, Пароль
  - Проверка силы пароля
  - Подтверждение пароля
  - Согласие с условиями

### Личный кабинет (dashboard.php)
- **URL:** http://localhost/scentury/dashboard.php
- **Функции:**
  - Информация о профиле
  - Быстрые действия
  - История заказов
  - Настройки аккаунта

### Восстановление пароля
- **Запрос:** http://localhost/scentury/forgot-password.php
- **Сброс:** http://localhost/scentury/reset-password.php?token=XXX

## 🔧 Технические детали

### Структура базы данных

```sql
-- Таблица пользователей
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    first_name VARCHAR(100) NOT NULL,
    last_name VARCHAR(100) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    phone VARCHAR(20),
    password VARCHAR(255) NOT NULL,
    newsletter BOOLEAN DEFAULT FALSE,
    status ENUM('active', 'inactive', 'banned') DEFAULT 'active',
    role ENUM('user', 'admin', 'moderator') DEFAULT 'user',
    remember_token VARCHAR(255) NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    last_login TIMESTAMP NULL
);
```

### Безопасность

1. **Хеширование паролей:**
   ```php
   $hashedPassword = password_hash($password, PASSWORD_DEFAULT);
   ```

2. **Валидация данных:**
   ```php
   if (!filter_var($email, FILTER_VALIDATE_EMAIL)) {
       // Ошибка валидации
   }
   ```

3. **Защита от SQL-инъекций:**
   ```php
   $stmt = $pdo->prepare("SELECT * FROM users WHERE email = ?");
   $stmt->execute([$email]);
   ```

4. **Токены восстановления:**
   ```php
   $token = bin2hex(random_bytes(32));
   ```

### Файловая система (резервный вариант)

Если база данных недоступна, система автоматически переключается на файловое хранение:

- **Файл пользователей:** `users.json`
- **Токены восстановления:** `password_resets.json`
- **Заказы:** `orders.txt`

## 🧪 Тестирование

### 1. Создание тестового пользователя

**Через форму регистрации:**
1. Откройте http://localhost/scentury/register.php
2. Заполните форму:
   - Имя: Тест
   - Фамилия: Пользователь
   - Email: test@scentury.ru
   - Пароль: password123
3. Нажмите "Зарегистрироваться"

**Через базу данных:**
```sql
INSERT INTO users (first_name, last_name, email, password, role) VALUES
('Тест', 'Пользователь', 'test@scentury.ru', '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', 'user');
```

### 2. Тестирование входа

1. Откройте http://localhost/scentury/login.php
2. Введите тестовые данные
3. Проверьте переход в личный кабинет

### 3. Тестирование восстановления пароля

1. Откройте http://localhost/scentury/forgot-password.php
2. Введите email тестового пользователя
3. Проверьте создание токена в файле `password_resets.json`

## 📋 Файлы системы

```
/auth-system/
├── login.php                    # Форма входа
├── register.php                 # Форма регистрации
├── dashboard.php                # Личный кабинет
├── forgot-password.php          # Запрос восстановления
├── reset-password.php           # Сброс пароля
├── password-reset.php           # Обработка запроса
├── password-reset-process.php   # Обработка сброса
├── auth.php                     # Система аутентификации
├── database.sql                 # Структура БД
├── auth-demo.html               # Демонстрация
└── AUTH_SETUP.md                # Эта инструкция
```

## ⚠️ Важные замечания

1. **Email отправка:** В демо-версии email не отправляется. Для продакшена настройте SMTP.

2. **Безопасность:** В продакшене используйте HTTPS и дополнительные меры безопасности.

3. **Валидация:** Все данные валидируются на клиенте и сервере.

4. **Сессии:** Система использует PHP сессии для управления состоянием.

5. **Токены:** Токены восстановления пароля действительны 1 час.

## 🎯 Готово к использованию!

Система авторизации полностью готова и протестирована. Все формы работают корректно как с базой данных, так и с файловым хранением.

**Для демонстрации откройте:** http://localhost/scentury/auth-demo.html

